services:
  - type: web
    name: automated-browser-vpn
    plan: standard  # Need standard plan for Docker
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: DOCKER_ENABLED
        value: true
